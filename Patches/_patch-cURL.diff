 lib/Makefile.m32   | 1 +
 lib/vtls/mbedtls.h | 3 +++
 src/Makefile.m32   | 1 +
 3 files changed, 5 insertions(+)

diff --git a/lib/Makefile.m32 b/lib/Makefile.m32
index 522dbd1df..a3bc7f765 100644
--- a/lib/Makefile.m32
+++ b/lib/Makefile.m32
@@ -310,6 +310,7 @@ ifndef USE_LDAP_OPENLDAP
 endif
 endif
 DLL_LIBS += -lws2_32
+DLL_LIBS += $(CURL_LDFLAG_EXTRAS2)
 
 # Makefile.inc provides the CSOURCES and HHEADERS defines
 include Makefile.inc
diff --git a/lib/vtls/mbedtls.h b/lib/vtls/mbedtls.h
index 71d17a491..adf3ce1e9 100644
--- a/lib/vtls/mbedtls.h
+++ b/lib/vtls/mbedtls.h
@@ -53,6 +53,9 @@ int Curl_mbedtls_shutdown(struct connectdata *conn, int sockindex);
 CURLcode Curl_mbedtls_random(struct Curl_easy *data, unsigned char *entropy,
                      size_t length);
 
+/* this backend supports the CAPATH option */
+#define have_curlssl_ca_path 1
+
 /* this backends supports CURLOPT_PINNEDPUBLICKEY */
 #define have_curlssl_pinnedpubkey 1
 
diff --git a/src/Makefile.m32 b/src/Makefile.m32
index 40852e573..10b281d15 100644
--- a/src/Makefile.m32
+++ b/src/Makefile.m32
@@ -331,6 +331,7 @@ curl_LDADD += -lwldap32
 endif
 endif
 curl_LDADD += -lws2_32
+curl_LDADD += $(CURL_LDFLAG_EXTRAS2)
 
 # Makefile.inc provides the CSOURCES and HHEADERS defines
 include Makefile.inc
